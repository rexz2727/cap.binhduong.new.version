// TODO: Task 8 — Button tái sử dụng (variant: primary | outline, hỗ trợ href và onClick)
export default function Button({ children }: { children: React.ReactNode }) {
  return <button>{children}</button>;
}
