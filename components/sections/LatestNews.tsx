// TODO: Task 9 — Section tin tức mới nhất (grid 3 cột NewsCard + link xem tất cả)
import type { NewsPostPreview } from "@/types/news";
export default function LatestNews({ posts }: { posts: NewsPostPreview[] }) {
  return <section />;
}
