# Full i18n (VI/EN) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Toàn bộ nội dung trang (UI chrome + CMS content từ Sanity) chuyển ngôn ngữ khi user bấm toggle EN/VI.

**Architecture:** UI chrome dùng hệ thống `t()` đã có trong `lib/i18n.tsx`. Nội dung CMS lưu thêm trường `*En` trong Sanity — admin bấm nút "Dịch sang EN" trong Studio để gọi Google Cloud Translation API 1 lần/document, kết quả lưu vào Sanity. Khi user chọn EN, component đọc trường EN từ Sanity (fallback về VI nếu chưa có).

**Tech Stack:** Next.js 16 App Router, React 19, Tailwind CSS v4, Sanity v5, Zod v4. Không có test runner — verify bằng `npm run build`.

**Spec:** `docs/superpowers/specs/2026-05-26-i18n-full-design.md`

---

## File Map

| File | Thay đổi |
|---|---|
| `lib/i18n.tsx` | Thêm keys còn thiếu cho ServiceGrid, UTILITY_ACTIONS |
| `constants/nav.ts` | HEADER_NAV_ITEMS + UTILITY_ACTIONS dùng key thay text cứng |
| `constants/services.ts` | SERVICE_CARDS label/description dùng i18n key |
| `components/layout/Header.tsx` | Dùng `t()` cho tất cả text còn hardcode |
| `components/layout/Footer.tsx` | Dùng `t()` cho text hardcode |
| `components/layout/MobileMenu.tsx` | Dùng `t()` cho text hardcode |
| `components/sections/ServiceGrid.tsx` | Dùng `t()` cho label/description |
| `components/sections/EmergencyRow.tsx` | Dùng `t()` |
| `components/sections/QuickLinks.tsx` | Dùng `t()` |
| `components/sections/ContactInfo.tsx` | Dùng `t()` |
| `sanity/schemas/newsPost.ts` | Thêm field `titleEn`, `excerptEn`, `bodyEn` |
| `sanity/schemas/legalDocument.ts` | Thêm field `titleEn`, `summaryEn` |
| `sanity/schemas/procedure.ts` | Thêm field `titleEn`, `summaryEn` |
| `sanity/schemas/announcement.ts` | Thêm field `textEn` |
| `sanity/schemas/personnel.ts` | Thêm field `positionEn`, `unitEn` |
| `types/news.ts` | Thêm `titleEn?`, `excerptEn?`, `bodyEn?` |
| `types/legalDocument.ts` | Thêm `titleEn?`, `summaryEn?` |
| `types/procedure.ts` | Thêm `titleEn?`, `summaryEn?` |
| `types/announcement.ts` | Thêm `textEn?` |
| `types/personnel.ts` | Thêm `positionEn?`, `unitEn?` |
| `sanity/lib/queries.ts` | Fetch thêm `*En` fields trong mọi query |
| `app/api/translate/route.ts` | Tạo mới — gọi Google Cloud Translation API |
| `sanity/actions/translateAction.tsx` | Tạo mới — Studio custom action |
| `sanity/actions/translateNewsAction.tsx` | Tạo mới — action cho newsPost |
| `sanity.config.ts` | Đăng ký translate actions |
| `app/(web)/tin-tuc/[slug]/page.tsx` | Hiển thị `titleEn`/`bodyEn` khi lang=en |
| `app/(web)/van-ban-phap-luat/[slug]/page.tsx` | Hiển thị `titleEn`/`summaryEn` khi lang=en |
| `app/(web)/thu-tuc-hanh-chinh/[slug]/page.tsx` | Hiển thị `titleEn`/`summaryEn` khi lang=en |
| `components/ui/NewsCard.tsx` | Hiển thị `titleEn`/`excerptEn` khi lang=en |
| `components/ui/LegalDocCard.tsx` | Hiển thị `titleEn` khi lang=en |
| `components/sections/LatestNews.tsx` | Pass lang context xuống NewsCard |
| `components/sections/NewsCarousel.tsx` | Pass lang context xuống NewsCard |
| `.env.local` | Thêm `GOOGLE_TRANSLATE_API_KEY` |

---

## Task 1: Thêm I18N keys còn thiếu

**Files:**
- Modify: `lib/i18n.tsx`
- Modify: `constants/services.ts`

- [ ] **Bước 1: Thêm keys vào `lib/i18n.tsx`**

Tìm cuối object `vi: {` và thêm trước dấu `}` đóng:
```ts
// Service grid
"svc.cccd": "Cấp CCCD",
"svc.cccd.desc": "Căn cước công dân",
"svc.residence": "Đăng ký cư trú",
"svc.residence.desc": "Thường trú, tạm trú",
"svc.vehicle": "Đăng ký xe",
"svc.vehicle.desc": "Mô tô, xe gắn máy",
"svc.passport": "Hộ chiếu",
"svc.passport.desc": "Cấp, gia hạn hộ chiếu",
"svc.appointment": "Đặt lịch hẹn",
"svc.appointment.desc": "Lịch tiếp công dân",
"svc.hoidap": "Hỏi đáp",
"svc.hoidap.desc": "Câu hỏi thường gặp",
// Utility actions
"util.schedule": "Lịch tiếp dân",
"util.feedback": "Phản ánh",
// Detail pages
"doc.issued": "Ngày ban hành",
"doc.effective": "Có hiệu lực",
"doc.issuer": "Cơ quan ban hành",
"doc.number": "Số hiệu",
"doc.download": "Xem / Tải văn bản (PDF)",
"doc.attach": "Tải văn bản đính kèm",
"doc.summary": "Tóm tắt nội dung",
"doc.viewPdf": "Xem PDF",
"common.backToList": "Quay lại danh sách",
"common.readMore": "Đọc thêm",
"common.publishedAt": "Ngày đăng",
"common.category": "Danh mục",
"common.noEn": "Nội dung chỉ có bằng tiếng Việt",
```

Tương tự thêm vào `en: {`:
```ts
"svc.cccd": "ID Card",
"svc.cccd.desc": "Citizen ID card",
"svc.residence": "Residence Registration",
"svc.residence.desc": "Permanent / temporary residence",
"svc.vehicle": "Vehicle Registration",
"svc.vehicle.desc": "Motorcycle, motorbike",
"svc.passport": "Passport",
"svc.passport.desc": "Issue or renew passport",
"svc.appointment": "Book Appointment",
"svc.appointment.desc": "Citizen reception schedule",
"svc.hoidap": "Q&A",
"svc.hoidap.desc": "Frequently asked questions",
"util.schedule": "Reception Schedule",
"util.feedback": "Submit Report",
"doc.issued": "Issued Date",
"doc.effective": "Effective Date",
"doc.issuer": "Issuing Body",
"doc.number": "Document No.",
"doc.download": "View / Download (PDF)",
"doc.attach": "Download attachment",
"doc.summary": "Summary",
"doc.viewPdf": "View PDF",
"common.backToList": "Back to list",
"common.readMore": "Read more",
"common.publishedAt": "Published",
"common.category": "Category",
"common.noEn": "Content available in Vietnamese only",
```

- [ ] **Bước 2: Cập nhật `constants/services.ts` dùng key**

```ts
import type { I18nKey } from "@/lib/i18n";

export interface ServiceCard {
  labelKey: I18nKey;
  descKey: I18nKey;
  href: string;
  icon: string;
}

export const SERVICE_CARDS: ServiceCard[] = [
  { labelKey: "svc.cccd", descKey: "svc.cccd.desc", href: "/thu-tuc-hanh-chinh", icon: "#i-doc" },
  { labelKey: "svc.residence", descKey: "svc.residence.desc", href: "/thu-tuc-hanh-chinh", icon: "#i-doc" },
  { labelKey: "svc.vehicle", descKey: "svc.vehicle.desc", href: "/thu-tuc-hanh-chinh", icon: "#i-shield" },
  { labelKey: "svc.passport", descKey: "svc.passport.desc", href: "/thu-tuc-hanh-chinh", icon: "#i-globe" },
  { labelKey: "svc.appointment", descKey: "svc.appointment.desc", href: "/lich-tiep-cong-dan", icon: "#i-cal" },
  { labelKey: "svc.hoidap", descKey: "svc.hoidap.desc", href: "/hoi-dap", icon: "#i-help" },
];
```

- [ ] **Bước 3: Cập nhật `components/sections/ServiceGrid.tsx`**

```tsx
"use client";
import Link from "next/link";
import { SERVICE_CARDS } from "@/constants/services";
import { useI18n } from "@/lib/i18n";

export default function ServiceGrid() {
  const { t } = useI18n();
  return (
    <div className="service-grid">
      {SERVICE_CARDS.map((card) => (
        <Link key={card.href + card.labelKey} href={card.href} className="service-card">
          <div className="sc-icon">
            <svg width="18" height="18" aria-hidden="true"><use href={card.icon} /></svg>
          </div>
          <span className="sc-label">{t(card.labelKey)}</span>
          <span className="sc-desc">{t(card.descKey)}</span>
        </Link>
      ))}
    </div>
  );
}
```

- [ ] **Bước 4: Cập nhật `constants/nav.ts` — UTILITY_ACTIONS dùng key**

```ts
import type { I18nKey } from "@/lib/i18n";

export interface UtilityAction {
  labelKey: I18nKey;
  href: string;
  icon: string;
  highlight?: boolean;
}

export const UTILITY_ACTIONS: UtilityAction[] = [
  { labelKey: "util.schedule", href: "/lich-tiep-cong-dan", icon: "#i-cal" },
  { labelKey: "util.feedback", href: "/phan-anh", icon: "#i-mail", highlight: true },
] as const;
```

- [ ] **Bước 5: Cập nhật Header.tsx render UTILITY_ACTIONS**

Tìm đoạn `{UTILITY_ACTIONS.map((action) => (` và sửa thành:
```tsx
{UTILITY_ACTIONS.map((action) => (
  <Link
    key={action.href}
    href={action.href}
    className={`util-action${action.highlight ? " highlight" : ""}`}
  >
    <svg width="12" height="12" aria-hidden="true"><use href={action.icon} /></svg>
    {t(action.labelKey)}
  </Link>
))}
```

- [ ] **Bước 6: Build check**
```bash
npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```
Expected: `✓ Compiled successfully`

- [ ] **Bước 7: Commit**
```bash
git add lib/i18n.tsx constants/services.ts constants/nav.ts components/sections/ServiceGrid.tsx components/layout/Header.tsx
git commit -m "feat(i18n): thêm keys còn thiếu, ServiceGrid + UTILITY_ACTIONS dùng t()"
```

---

## Task 2: Fix hardcoded strings — Footer, MobileMenu, EmergencyRow

**Files:**
- Modify: `components/layout/Footer.tsx`
- Modify: `components/layout/MobileMenu.tsx`
- Modify: `components/sections/EmergencyRow.tsx`

- [ ] **Bước 1: Đọc Footer.tsx tìm hardcoded strings**

Tìm tất cả text tiếng Việt cứng trong Footer (section headings, labels). Thay bằng `t()` dùng keys đã có:
- "Liên kết nhanh" → `t("footer.h.links")`
- "Cơ quan liên quan" → `t("footer.h.agencies")`
- "Theo dõi chúng tôi" → `t("footer.h.social")`
- "Đường dây khẩn cấp" → `t("footer.h.emergency")`
- "Phản ánh tội phạm 24/7" → giữ nguyên (số điện thoại, không dịch)
- Thêm `const { t } = useI18n();` nếu chưa có

- [ ] **Bước 2: Đọc MobileMenu.tsx tìm hardcoded strings**

Tìm text cứng trong MobileMenu. Thay bằng `t()`:
- CTA "Phản ánh" → `t("util.feedback")`
- Thêm `const { t } = useI18n();` nếu chưa có

- [ ] **Bước 3: Đọc EmergencyRow.tsx tìm hardcoded strings**

Thay text cứng bằng keys đã có:
- "Trường hợp khẩn cấp gọi ngay" → `t("emergency.title")`
- "Cảnh sát" → `t("emergency.police")`
- "Cứu hỏa" → `t("emergency.fire")`
- "Cấp cứu y tế" → `t("emergency.medical")`

- [ ] **Bước 4: Build check**
```bash
npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```

- [ ] **Bước 5: Commit**
```bash
git add components/layout/Footer.tsx components/layout/MobileMenu.tsx components/sections/EmergencyRow.tsx
git commit -m "feat(i18n): Footer, MobileMenu, EmergencyRow dùng t()"
```

---

## Task 3: Fix hardcoded strings — QuickLinks, ContactInfo

**Files:**
- Modify: `components/sections/QuickLinks.tsx`
- Modify: `components/sections/ContactInfo.tsx`

- [ ] **Bước 1: Đọc QuickLinks.tsx**

Tìm text cứng. Thay bằng `t()`. Các keys liên quan:
- "Dịch vụ trực tuyến" → `t("section.services.eye")`
- "Truy cập nhanh các dịch vụ công" → `t("section.services.title")`
- Labels của từng link dùng keys `svc.*` đã có

- [ ] **Bước 2: Đọc ContactInfo.tsx**

Thay bằng `t()`:
- "Địa chỉ" → `t("contact.address.label")`
- "Điện thoại" → `t("contact.phone.label")`
- "Email" → `t("contact.email.label")`
- "Giờ làm việc" → `t("contact.hours.label")`

- [ ] **Bước 3: Build check + commit**
```bash
npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
git add components/sections/QuickLinks.tsx components/sections/ContactInfo.tsx
git commit -m "feat(i18n): QuickLinks, ContactInfo dùng t()"
```

---

## Task 4: Sanity schemas — Thêm trường EN

**Files:**
- Modify: `sanity/schemas/newsPost.ts`
- Modify: `sanity/schemas/legalDocument.ts`
- Modify: `sanity/schemas/procedure.ts`
- Modify: `sanity/schemas/announcement.ts`
- Modify: `sanity/schemas/personnel.ts`

- [ ] **Bước 1: Thêm vào `sanity/schemas/newsPost.ts`**

Sau field `body`, thêm:
```ts
defineField({
  name: "titleEn",
  title: "Tiêu đề (EN)",
  type: "string",
  group: "english",
}),
defineField({
  name: "excerptEn",
  title: "Tóm tắt (EN)",
  type: "text",
  rows: 3,
  group: "english",
}),
defineField({
  name: "bodyEn",
  title: "Nội dung (EN)",
  type: "array",
  of: [{ type: "block" }],
  group: "english",
}),
```

Thêm group ở đầu schema (sau `name: "newsPost"`):
```ts
groups: [
  { name: "main", title: "Nội dung chính", default: true },
  { name: "english", title: "🇬🇧 Tiếng Anh" },
],
```

Thêm `group: "main"` vào các field hiện có: `title`, `slug`, `excerpt`, `body`, `mainImage`, `publishedAt`, `category`, `isFeatured`.

- [ ] **Bước 2: Thêm vào `sanity/schemas/legalDocument.ts`**

Sau field `body`, thêm:
```ts
defineField({ name: "titleEn", title: "Tiêu đề (EN)", type: "string" }),
defineField({ name: "summaryEn", title: "Tóm tắt (EN)", type: "text", rows: 4 }),
```

- [ ] **Bước 3: Thêm vào `sanity/schemas/procedure.ts`**

Sau field `steps`, thêm:
```ts
defineField({ name: "titleEn", title: "Tên thủ tục (EN)", type: "string" }),
defineField({ name: "summaryEn", title: "Mô tả ngắn (EN)", type: "text", rows: 3 }),
```

- [ ] **Bước 4: Thêm vào `sanity/schemas/announcement.ts`**

Sau field `text`, thêm:
```ts
defineField({ name: "textEn", title: "Nội dung (EN)", type: "string" }),
```

- [ ] **Bước 5: Thêm vào `sanity/schemas/personnel.ts`**

Tìm field `position` và `unit`, thêm sau chúng:
```ts
defineField({ name: "positionEn", title: "Chức vụ (EN)", type: "string" }),
defineField({ name: "unitEn", title: "Đơn vị (EN)", type: "string" }),
```

- [ ] **Bước 6: Build check**
```bash
npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
```

- [ ] **Bước 7: Commit**
```bash
git add sanity/schemas/newsPost.ts sanity/schemas/legalDocument.ts sanity/schemas/procedure.ts sanity/schemas/announcement.ts sanity/schemas/personnel.ts
git commit -m "feat(sanity): thêm trường EN cho newsPost, legalDocument, procedure, announcement, personnel"
```

---

## Task 5: TypeScript types — Thêm optional EN fields

**Files:**
- Modify: `types/news.ts`
- Modify: `types/legalDocument.ts`
- Modify: `types/procedure.ts`
- Modify: `types/announcement.ts`
- Modify: `types/personnel.ts`

- [ ] **Bước 1: Cập nhật `types/news.ts`**

Trong interface `NewsPost`, thêm sau `body`:
```ts
titleEn?: string;
excerptEn?: string;
bodyEn?: import("@portabletext/types").PortableTextBlock[];
```

- [ ] **Bước 2: Cập nhật `types/legalDocument.ts`**

Trong interface `LegalDocument`, thêm sau `body?`:
```ts
titleEn?: string;
summaryEn?: string;
```

- [ ] **Bước 3: Cập nhật `types/procedure.ts`**

Trong interface `Procedure`, thêm sau `forms?`:
```ts
titleEn?: string;
summaryEn?: string;
```

- [ ] **Bước 4: Cập nhật `types/announcement.ts`**

Trong interface `Announcement`, thêm sau `text`:
```ts
textEn?: string;
```

- [ ] **Bước 5: Cập nhật `types/personnel.ts`**

Trong interface `Personnel`, thêm sau `unit`:
```ts
positionEn?: string;
unitEn?: string;
```

- [ ] **Bước 6: Build check + commit**
```bash
npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
git add types/news.ts types/legalDocument.ts types/procedure.ts types/announcement.ts types/personnel.ts
git commit -m "feat(types): thêm optional EN fields vào TypeScript interfaces"
```

---

## Task 6: GROQ Queries — Fetch thêm EN fields

**Files:**
- Modify: `sanity/lib/queries.ts`

> Đọc toàn bộ file trước để hiểu pattern, sau đó cập nhật từng query.

- [ ] **Bước 1: Cập nhật `getNewsPosts` và `getNewsPostBySlug`**

Trong query `getNewsPostBySlug`, thêm `titleEn`, `excerptEn`, `bodyEn` vào projection:
```groq
*[_type == "newsPost" && slug.current == $slug][0]{
  ...,
  titleEn,
  excerptEn,
  bodyEn,
  "attachedFileUrl": attachedFile.asset->url
}
```

Trong `getNewsPosts` (list), thêm `titleEn`, `excerptEn` vào projection nếu có explicit fields, hoặc dùng `...` để tự include.

- [ ] **Bước 2: Cập nhật `getLegalDocBySlug` và `getLegalDocuments`**

```groq
*[_type == "legalDocument" && slug.current == $slug][0]{
  ...,
  titleEn,
  summaryEn,
  "attachedFileUrl": attachedFile.asset->url
}
```

- [ ] **Bước 3: Cập nhật `getProcedureBySlug` và `getProcedures`**

Thêm `titleEn`, `summaryEn` vào projection.

- [ ] **Bước 4: Cập nhật `getAnnouncements`**

Thêm `textEn` vào projection.

- [ ] **Bước 5: Cập nhật `getPersonnel`**

Thêm `positionEn`, `unitEn` vào projection.

- [ ] **Bước 6: Build check + commit**
```bash
npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
git add sanity/lib/queries.ts
git commit -m "feat(queries): fetch EN fields trong tất cả GROQ queries"
```

---

## Task 7: API Route /api/translate

**Files:**
- Create: `app/api/translate/route.ts`

> QUAN TRỌNG: Route này chỉ dùng từ Sanity Studio (server-side action). Không expose cho public. Validate bằng secret header.

- [ ] **Bước 1: Thêm env var**

Thêm vào `.env.local`:
```
GOOGLE_TRANSLATE_API_KEY=your_google_cloud_api_key
TRANSLATE_SECRET=random_secret_string_min_32_chars
```

- [ ] **Bước 2: Tạo `app/api/translate/route.ts`**

```ts
import { NextRequest, NextResponse } from "next/server";
import { z } from "zod";

const bodySchema = z.object({
  texts: z.array(z.string().min(1).max(5000)).min(1).max(20),
  source: z.literal("vi"),
  target: z.literal("en"),
});

export async function POST(req: NextRequest) {
  const secret = req.headers.get("x-translate-secret");
  if (secret !== process.env.TRANSLATE_SECRET) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  const parsed = bodySchema.safeParse(await req.json());
  if (!parsed.success) {
    return NextResponse.json({ error: "Invalid input" }, { status: 400 });
  }

  const { texts, source, target } = parsed.data;
  const apiKey = process.env.GOOGLE_TRANSLATE_API_KEY;
  if (!apiKey) {
    return NextResponse.json({ error: "Translation not configured" }, { status: 503 });
  }

  const res = await fetch(
    `https://translation.googleapis.com/language/translate/v2?key=${apiKey}`,
    {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ q: texts, source, target, format: "text" }),
    }
  );

  if (!res.ok) {
    return NextResponse.json({ error: "Translation API error" }, { status: 502 });
  }

  const data = await res.json();
  const translated: string[] = data.data.translations.map(
    (t: { translatedText: string }) => t.translatedText
  );

  return NextResponse.json({ translated });
}
```

- [ ] **Bước 3: Build check + commit**
```bash
npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
git add app/api/translate/route.ts
git commit -m "feat(api): POST /api/translate — Google Cloud Translation với auth secret"
```

---

## Task 8: Sanity Studio — Custom Translate Action

**Files:**
- Create: `sanity/actions/translateAction.tsx`
- Modify: `sanity.config.ts`

- [ ] **Bước 1: Tạo `sanity/actions/translateAction.tsx`**

```tsx
import { useState } from "react";
import { useToast } from "@sanity/ui";
import { TranslateIcon } from "@sanity/icons";
import { useDocumentOperation, useCurrentUser } from "sanity";

type FieldMap = { from: string; to: string }[];

async function callTranslate(texts: string[]): Promise<string[]> {
  const res = await fetch("/api/translate", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "x-translate-secret": process.env.SANITY_STUDIO_TRANSLATE_SECRET ?? "",
    },
    body: JSON.stringify({ texts, source: "vi", target: "en" }),
  });
  if (!res.ok) throw new Error("Translation failed");
  const data = await res.json();
  return data.translated as string[];
}

export function createTranslateAction(fieldMap: FieldMap) {
  return function TranslateAction({ id, type, published }: { id: string; type: string; published: unknown }) {
    const { patch, commit } = useDocumentOperation(id, type);
    const toast = useToast();
    const [isTranslating, setIsTranslating] = useState(false);

    const doc = published as Record<string, string> | null;
    if (!doc) return null;

    return {
      label: isTranslating ? "Đang dịch…" : "🌐 Dịch sang EN",
      icon: TranslateIcon,
      disabled: isTranslating,
      onHandle: async () => {
        setIsTranslating(true);
        try {
          const sourceTexts = fieldMap.map((f) => (doc[f.from] as string) ?? "");
          const translated = await callTranslate(sourceTexts);
          const patchObj: Record<string, string> = {};
          fieldMap.forEach((f, i) => {
            if (translated[i]) patchObj[f.to] = translated[i];
          });
          patch.execute([{ set: patchObj }]);
          commit.execute();
          toast.push({ status: "success", title: "Đã dịch — hãy review trước khi publish" });
        } catch {
          toast.push({ status: "error", title: "Lỗi dịch thuật. Thử lại sau." });
        } finally {
          setIsTranslating(false);
        }
      },
    };
  };
}

export const translateNewsAction = createTranslateAction([
  { from: "title", to: "titleEn" },
  { from: "excerpt", to: "excerptEn" },
]);

export const translateLegalAction = createTranslateAction([
  { from: "title", to: "titleEn" },
  { from: "summary", to: "summaryEn" },
]);

export const translateProcedureAction = createTranslateAction([
  { from: "title", to: "titleEn" },
  { from: "summary", to: "summaryEn" },
]);

export const translatePersonnelAction = createTranslateAction([
  { from: "position", to: "positionEn" },
  { from: "unit", to: "unitEn" },
]);
```

- [ ] **Bước 2: Thêm env var cho Studio**

Thêm vào `.env.local`:
```
SANITY_STUDIO_TRANSLATE_SECRET=same_value_as_TRANSLATE_SECRET
```

- [ ] **Bước 3: Đăng ký actions trong `sanity.config.ts`**

Đọc file `sanity.config.ts`, tìm `defineConfig({`, thêm `document.actions`:
```ts
import {
  translateNewsAction,
  translateLegalAction,
  translateProcedureAction,
  translatePersonnelAction,
} from "./sanity/actions/translateAction";

// Trong defineConfig:
document: {
  actions: (prev, { schemaType }) => {
    const actionMap: Record<string, ReturnType<typeof createTranslateAction>> = {
      newsPost: translateNewsAction,
      legalDocument: translateLegalAction,
      procedure: translateProcedureAction,
      personnel: translatePersonnelAction,
    };
    const translateAction = actionMap[schemaType];
    return translateAction ? [...prev, translateAction] : prev;
  },
},
```

- [ ] **Bước 4: Build check + commit**
```bash
npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
git add sanity/actions/translateAction.tsx sanity.config.ts
git commit -m "feat(studio): custom action Dịch sang EN cho newsPost, legalDocument, procedure, personnel"
```

---

## Task 9: Bilingual display — Detail pages

**Files:**
- Modify: `app/(web)/tin-tuc/[slug]/page.tsx`
- Modify: `app/(web)/van-ban-phap-luat/[slug]/page.tsx`
- Modify: `app/(web)/thu-tuc-hanh-chinh/[slug]/page.tsx`

> Pattern chung: Đây là Server Components. Cần truyền `lang` từ cookie/header hoặc đọc từ searchParams. Dùng cookie `lang` (đã được set bởi LanguageProvider trong localStorage — cần đồng bộ sang cookie).

- [ ] **Bước 1: Thêm helper `lib/getLang.ts`**

```ts
import { cookies } from "next/headers";
import type { Lang } from "@/lib/i18n";

export async function getLang(): Promise<Lang> {
  const cookieStore = await cookies();
  const lang = cookieStore.get("lang")?.value;
  return lang === "en" ? "en" : "vi";
}
```

- [ ] **Bước 2: Cập nhật `LanguageProvider` trong `lib/i18n.tsx` để sync cookie**

Trong `toggleLang`, thêm sau `localStorage.setItem`:
```ts
document.cookie = `lang=${nextLang};path=/;max-age=31536000`;
```

Trong `useEffect` khởi tạo, thêm:
```ts
document.cookie = `lang=${savedLang};path=/;max-age=31536000`;
```

- [ ] **Bước 3: Cập nhật `app/(web)/tin-tuc/[slug]/page.tsx`**

```tsx
import { getLang } from "@/lib/getLang";

export default async function NewsDetailPage({ params }: Props) {
  const { slug } = await params;
  const [post, lang] = await Promise.all([getNewsPostBySlug(slug), getLang()]);
  if (!post) notFound();

  const title = lang === "en" && post.titleEn ? post.titleEn : post.title;
  const body = lang === "en" && post.bodyEn ? post.bodyEn : post.body;
  // Dùng title, body thay vì post.title, post.body trong JSX
}
```

- [ ] **Bước 4: Cập nhật `app/(web)/van-ban-phap-luat/[slug]/page.tsx`**

```tsx
import { getLang } from "@/lib/getLang";

export default async function LegalDocDetailPage({ params }: Props) {
  const { slug } = await params;
  const [doc, lang] = await Promise.all([getLegalDocBySlug(slug), getLang()]);
  if (!doc) notFound();

  const title = lang === "en" && doc.titleEn ? doc.titleEn : doc.title;
  const summary = lang === "en" && doc.summaryEn ? doc.summaryEn : doc.summary;
  // Dùng title, summary trong JSX
}
```

- [ ] **Bước 5: Cập nhật `app/(web)/thu-tuc-hanh-chinh/[slug]/page.tsx`**

Tương tự pattern trên với `titleEn`, `summaryEn`.

- [ ] **Bước 6: Build check + commit**
```bash
npm run build 2>&1 | grep -E "error|Error|✓ Compiled"
git add lib/getLang.ts lib/i18n.tsx app/\(web\)/tin-tuc/\[slug\]/page.tsx app/\(web\)/van-ban-phap-luat/\[slug\]/page.tsx app/\(web\)/thu-tuc-hanh-chinh/\[slug\]/page.tsx
git commit -m "feat(i18n): detail pages hiển thị EN content khi lang=en"
```

---

## Task 10: Bilingual display — List components

**Files:**
- Modify: `components/ui/NewsCard.tsx`
- Modify: `components/ui/LegalDocCard.tsx`
- Modify: `components/sections/LatestNews.tsx`
- Modify: `components/sections/NewsCarousel.tsx`
- Modify: `components/layout/NewsTicker.tsx`

> Các component này là Client Components (dùng `useI18n()`).

- [ ] **Bước 1: Cập nhật `components/ui/NewsCard.tsx`**

Thêm `useI18n`:
```tsx
"use client";
import { useI18n } from "@/lib/i18n";

export default function NewsCard({ post }: { post: NewsPostPreview & { titleEn?: string; excerptEn?: string } }) {
  const { lang } = useI18n();
  const title = lang === "en" && post.titleEn ? post.titleEn : post.title;
  const excerpt = lang === "en" && post.excerptEn ? post.excerptEn : post.excerpt;
  // Dùng title, excerpt trong JSX thay vì post.title, post.excerpt
}
```

- [ ] **Bước 2: Cập nhật `components/ui/LegalDocCard.tsx`**

```tsx
"use client";
import { useI18n } from "@/lib/i18n";

export default function LegalDocCard({ doc }: { doc: LegalDocumentPreview & { titleEn?: string } }) {
  const { lang } = useI18n();
  const title = lang === "en" && doc.titleEn ? doc.titleEn : doc.title;
  // Dùng title trong JSX
}
```

- [ ] **Bước 3: Cập nhật `LegalDocumentPreview` type**

Trong `types/legalDocument.ts`, thêm `titleEn` vào Pick của `LegalDocumentPreview`:
```ts
export type LegalDocumentPreview = Pick<
  LegalDocument,
  "_id" | "title" | "titleEn" | "slug" | "documentNumber" | "issuedDate" | "category" | "issuingBody"
>;
```

Cập nhật GROQ query `getLegalDocuments` để fetch `titleEn`.

- [ ] **Bước 4: Cập nhật `NewsTicker.tsx`**

```tsx
const { lang, t } = useI18n();
// Khi render text ticker: dùng lang === "en" && item.textEn ? item.textEn : item.text
```

- [ ] **Bước 5: Build check cuối**
```bash
npm run build 2>&1 | tail -20
```
Expected: 0 errors, `✓ Compiled successfully`

- [ ] **Bước 6: Commit + push**
```bash
git add components/ui/NewsCard.tsx components/ui/LegalDocCard.tsx components/sections/LatestNews.tsx components/sections/NewsCarousel.tsx components/layout/NewsTicker.tsx types/legalDocument.ts
git commit -m "feat(i18n): NewsCard, LegalDocCard, NewsTicker hiển thị EN khi lang=en"
git push origin main
```

---

## Checklist cuối

- [ ] Toggle EN/VI chuyển toàn bộ UI chrome
- [ ] Admin bấm "🌐 Dịch sang EN" trong Studio → các trường EN được điền
- [ ] Trang chi tiết (news, legal, procedure) hiển thị EN khi lang=en
- [ ] List cards hiển thị tiêu đề EN khi lang=en
- [ ] Fallback về VI khi chưa có bản EN
- [ ] `npm run build` pass 0 lỗi
- [ ] `GOOGLE_TRANSLATE_API_KEY` và `TRANSLATE_SECRET` đã set trong `.env.local`
